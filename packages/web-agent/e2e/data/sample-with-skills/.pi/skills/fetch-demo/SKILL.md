---
name: fetch-demo
description: Demonstrate the sandbox fetch() capability by GETting a URL passed as $ARGUMENTS.
---

# Fetch Demo

Run `node fetch.js <url>` to perform an HTTP GET from inside the sandbox.
Prints the response status and the first 200 characters of the body.
